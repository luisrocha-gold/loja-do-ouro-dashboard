# Dashboard protection (server-only)
DASHBOARD_USER=
DASHBOARD_PASSWORD=
DASHBOARD_SESSION_SECRET=

# Nightly validation / refresh job (server-only)
CRON_SECRET=

# Windsor.ai (server-only)
WINDSOR_API_KEY=
WINDSOR_SHOPIFY_ACCOUNT_ID=lojadoouro-online.myshopify.com
WINDSOR_META_ACCOUNT_ID=189245068300417
WINDSOR_GOOGLE_ACCOUNT_ID=266-236-4039

# Direct connector mode is available while the private BI reader is unconfigured.
# Never expose server secrets with NEXT_PUBLIC_.

# Private BI reader. Only used server-side; never use NEXT_PUBLIC_ credentials.
# A JWT for an existing ldo_bi_members user preserves database RLS.
BI_SUPABASE_URL=https://eblxrzsjocvzjewrapnh.supabase.co
BI_SUPABASE_PUBLISHABLE_KEY=
BI_SUPABASE_ACCESS_TOKEN=
# Alternatively an existing server-only SUPABASE_SERVICE_ROLE_KEY is supported.
# Prefer a BI member credential and arrange its renewal; expired tokens fail closed.
